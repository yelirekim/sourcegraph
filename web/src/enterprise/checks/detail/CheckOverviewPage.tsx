import * as React from 'react'
import { Check } from '../data'

interface Props {
    check: Check
}

/**
 * The overview page for a single check.
 */
export const CheckOverviewPage: React.FunctionComponent<Props> = props => (
    <div className="check-overview-page">asdf</div>
)
